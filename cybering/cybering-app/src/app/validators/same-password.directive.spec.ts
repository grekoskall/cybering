import { SamePasswordDirective } from './same-password.directive';

describe('SamePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new SamePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
